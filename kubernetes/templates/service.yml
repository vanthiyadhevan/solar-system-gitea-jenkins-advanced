apiVersion: v1
kind: Service
metadata:
  name: {{ include "mychart.name" . }}
spec:
  selector:
    app: {{ include "mychart.name" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
